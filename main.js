$(document).ready(function(x) {
  if ((w = $(window).width()) >= 600) w = w*0.5;
  h = $(window).height();
  $("body").append(containers);
  getLit($("#content"),lit);
  //for (var i=0; i<=4; i++)
  	poems[0];
  	poems[1];
  	poems[2];
  	poems[3];
});